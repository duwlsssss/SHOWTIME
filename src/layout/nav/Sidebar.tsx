import styled from 'styled-components';

const SidebarContainer = styled.aside`
	padding: 20px;
`;

function Sidebar() {
	return <SidebarContainer>테스트 사이드바</SidebarContainer>;
}

export default Sidebar;

import styled from 'styled-components';

const SalaryPage = styled.div.attrs({ className: 'page-content' })``;

function SalaryDetails() {
	return (
		<SalaryPage>
			<h2>Salary Details</h2>
			{/* 급여 상세 페이지 내용 */}
		</SalaryPage>
	);
}

export default SalaryDetails;
